---
title: 'Story 2.3 — Cameras appear as devices under one server hub'
type: 'feature'
created: '2026-09-12'
status: 'done'
baseline_revision: '4f52d064c3dd30dfcdecfb1785a406120ad14491'
final_revision: ''
review_loop_iteration: 1
followup_review_recommended: false
context:
  - '{project-root}/docs/ha-integration-reference.md'
  - '{project-root}/_bmad-output/implementation-artifacts/epic-2-context.md'
  - '{project-root}/_bmad-output/planning-artifacts/architecture/architecture-ha-securityspy-2026-08-09/ARCHITECTURE-SPINE.md'
warnings: []
---

<intent-contract>

## Intent

**Problem:** Stories 2.1 and 2.2 can create and validate a config entry, but `PLATFORMS` is empty and no device or entity of any kind exists yet. A user who adds their server sees a config entry and nothing else — eleven cameras stay invisible. This is also the story every later Epic-2 story is blocked on: 2.4's health sensors, 2.5's update entity, 2.6's camera stream, and 2.7's permission gating are all written against a hub device, per-camera devices, and a coordinator that this story alone creates (confirmed by the halted 2.4 auto-dev run, which refused to invent this scope itself).

**Approach:** Add the coordinator (`coordinator.py`) and the entity base module (`entity.py`) the architecture names as the two permanent seams (AD-4, AD-5), and use them to register exactly one hub device and one device per inventoried camera through the device registry — directly, with no platform entity yet, since no per-camera data worth exposing as an entity exists until story 2.4. The coordinator also owns a slow periodic re-fetch of `async_get_server_info()` so camera renames, additions, and removals reconcile without a manual reload, which the epic's acceptance criteria require even before Epic 3's push stream exists.

## Boundaries & Constraints

**Always:**
- Identity is exactly AD-5, verified against `ARCHITECTURE-SPINE.md`: hub `identifiers={(DOMAIN, server.uuid)}`, `entry_type=DeviceEntryType.SERVICE`; camera `identifiers={(DOMAIN, f"{server.uuid}_{camera.number}")}`, `via_device=(DOMAIN, server.uuid)`. Never hostname, IP, camera name, or a fabricated MAC; `connections` is never populated.
- `server.uuid` and `camera.number` are the only two facts identity may depend on. Both already exist on `entry.runtime_data.server` (`ServerInfo`, from `async_get_server_info()` in `__init__.py`) — no new library call is added for identity.
- Exactly one hub device, and exactly one camera device per entry in `server.cameras` (a `Mapping[int, Camera]`, already includes every camera the account's inventory call returns) — **not** filtered by `camera.enabled` or by permission. Permission-based *entity* gating is story 2.7's job and does not apply to devices; the AC for this story tests device existence only.
- The coordinator (`SecuritySpyDataUpdateCoordinator(DataUpdateCoordinator[ServerInfo])`) is constructed with `update_interval=None` (AD-4) — the only interval-driven behavior this story adds is a self-scheduled reconciliation timer via `async_track_time_interval`, started explicitly by the coordinator itself, never the base class's own polling.
- One coordinator per config entry (AD-4), stored on `entry.runtime_data` alongside the existing `client` (extend `SecuritySpyRuntimeData`, do not replace it — 2.1/2.2 already rely on `.client` and `.server`).
- Device registration and reconciliation run once at startup (from `async_setup_entry`, before `async_forward_entry_setups`) and on every subsequent scheduled re-fetch — never inside an entity, since no entity performs its own I/O (AD-4).
- A camera renamed on the server updates the device's `name` via `device_registry.async_update_device()` on the next reconciliation; its device `id`, its `identifiers`, and (once they exist) any entity `unique_id` are untouched.
- A camera added to the inventory gets a new device on the next reconciliation; a camera removed from the inventory gets its device removed via `device_registry.async_remove_device()` — device removal must not touch the hub or any other camera's device.
- `entity.py` is introduced now, exporting the pure `hub_device_info(server) -> DeviceInfo` and `camera_device_info(server, camera) -> DeviceInfo` builder functions AD-5 requires, so the coordinator and every future platform module compute identity from one place. No entity *class* exists yet — that starts in 2.4 — but the module and its two functions must exist under this name for `common-modules` (Bronze) to be satisfiable when platforms do arrive, and so the identity computation is written exactly once.
- `manufacturer="Ben Software"` and `sw_version=server.version` on the hub; the camera device carries no `sw_version` (SecuritySpy does not report one per camera) and no `model` (the library's `Camera` model has no model field — do not invent one).
- Diagnostic and Repair conventions untouched by this story: no options flow, no repair issue, no diagnostics platform change (`diagnostics.py` does not exist yet and is out of scope).

**Block If:**
- Nothing new is blocked; everything this story needs (`ServerInfo.uuid`, `ServerInfo.cameras`, `Camera.number`, `Camera.name`) already ships in `aiosecurityspy` (stories 1.12, 1.18).

**Never:**
- No platform module (`sensor.py`, `binary_sensor.py`, etc.) and no `Platform` entry added to `PLATFORMS` — there is no per-camera data yet worth exposing as an entity (health is 2.4, the update entity is 2.5, live video is 2.6). Devices with zero entities are a supported, documented Home Assistant pattern (`device_registry.async_get_or_create` does not require an entity to exist) and are exactly what this story's AC asks for.
- No permission gating (2.7), no reauth or auth-failure counter (2.8, already partially covered by 2.1's error mapping — untouched here), no reconfigure flow (2.9).
- No use of the library's event stream — it does not exist yet (Epic 3). The reconciliation timer this story adds is a stand-in the stream will eventually make redundant for the "no reload needed" AC; do not attempt to build stream-driven reconciliation here.
- No change to `config_flow.py` or to any `strings.json`/`translations/en.json` key — this story has no new user-facing flow surface.
- No `SecuritySpyData` typed container in the library. `ServerInfo` already satisfies what AD-15 asks of the coordinator's data at this stage — a frozen, fully-typed container with `cameras: Mapping[int, Camera]` keyed by `int`. Introducing a wrapping library type before a later epic actually needs additional fields (episodes, latest capture, control state) would be speculative; extending or wrapping `ServerInfo` when that need arrives is cheap. Flagged as a design decision for review, not settled by the epic context.
- No `async_get_or_create` call outside the coordinator. Only the coordinator writes to the device registry, mirroring "the coordinator is the sole writer" even though there is no `SecuritySpyData` write happening yet — the invariant is about I/O ownership, not the specific method name.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|---|---|---|---|
| Fresh setup, N cameras | `server.cameras` has N entries | Exactly 1 hub device (`entry_type=SERVICE`) + N camera devices, each `via_device` the hub | No error expected |
| Zero cameras | `server.cameras` is empty | Hub device only; no camera devices; setup still succeeds | No error expected |
| Camera renamed on the server | Next reconciliation sees a new `camera.name` for an existing `camera.number` | Device `name` updates | No error; device `id`/`identifiers` unchanged |
| Camera added on the server | Next reconciliation sees a `camera.number` not yet in the device registry for this entry | New camera device created, `via_device` the hub | No error expected |
| Camera removed on the server | Next reconciliation no longer sees a previously-known `camera.number` | That camera's device is removed; hub and other camera devices untouched | No error expected |
| Host/IP/port changed, same server | Config entry reloaded (2.9 territory, but must not regress here) | No duplicate hub or camera devices — identity keys off `server.uuid`/`camera.number` only | No error expected |
| Reconciliation poll fails transiently | `async_get_server_info()` raises `SecuritySpyConnectError` on a scheduled re-fetch | Device registry is left as-is; failure logged once at `DEBUG`; the *next* scheduled attempt retries | No exception escapes the timer callback; nothing crashes the event loop |
| Reconciliation poll fails permanently | `async_get_server_info()` raises `SecuritySpyAuthError`/`SecuritySpyUnsupportedVersionError` on a scheduled re-fetch | Same as transient for *this story* — the auth-failure counter and reauth flow are story 2.8's job, not introduced here | Logged once at `DEBUG`; devices untouched |
| Unload | `async_unload_entry` runs | The reconciliation timer is cancelled; devices are **not** removed (Home Assistant's own config-entry removal handles that) | No leaked timer (FR-33) |

</intent-contract>

## Code Map

- `custom_components/securityspy/__init__.py` — `async_setup_entry()` currently builds `client`, calls `async_get_server_info()`, and sets `entry.runtime_data = SecuritySpyRuntimeData(client=client, server=server)` (lines ~117-197 as of this baseline). This story adds coordinator construction and the first device-registry sync **after** that assignment and **before** `await hass.config_entries.async_forward_entry_setups(entry, PLATFORMS)` (`PLATFORMS` stays `[]`). `async_unload_entry()` must cancel the coordinator's reconciliation timer.
- `custom_components/securityspy/coordinator.py` — **new file.** `SecuritySpyDataUpdateCoordinator(DataUpdateCoordinator[ServerInfo])`; owns the device-registry sync and the reconciliation timer. Update the module docstring in `__init__.py` (it currently says "No coordinator, platform or entity exists yet — `PLATFORMS` is deliberately empty until story 2.3 introduces them") to describe the post-2.3 state accurately: coordinator and `entity.py` exist, `PLATFORMS` remains empty until 2.4.
- `custom_components/securityspy/entity.py` — **new file.** `hub_device_info(server: ServerInfo) -> DeviceInfo` and `camera_device_info(server: ServerInfo, camera: Camera) -> DeviceInfo` per AD-5. Pure functions, no HA I/O, no entity classes yet.
- `custom_components/securityspy/const.py` — add a `RECONCILE_INTERVAL` constant (`timedelta`, see Design Notes for the value and its source) so the interval is named once and importable by tests.
- `homeassistant.helpers.device_registry` — new dependency for this module: `async_get(hass)`, `async_get_or_create(config_entry_id=entry.entry_id, identifiers=..., ...)`, `async_update_device(device_id, name=...)`, `async_remove_device(device_id)`. Look these up against the installed `homeassistant` package's `helpers/device_registry.py` before writing calls — do not guess the current signature from memory.
- `docs/ha-integration-reference.md` §§2-4 — the target end-state shapes for `SecuritySpyRuntimeData`, the coordinator, and `DeviceInfo` construction. That doc describes the *full* eventual coordinator (stream-fed, `SecuritySpyData`-typed); this story implements a deliberately smaller slice of it (no stream, `ServerInfo`-typed) and should not be over-built to match the doc's end state prematurely.
- `tests/conftest.py` — `MOCK_USER_INPUT`, `SERVER_UUID`, `SERVER_NAME` already exist; a fixture producing a `ServerInfo` with a small camera map (e.g. 2-3 `Camera` entries) is needed for coordinator/device tests and does not yet exist — check before adding a duplicate.
- `tests/test_init.py` — existing setup tests assert on `entry.runtime_data.client`/`.server`; extend rather than restructure. New coverage: coordinator is constructed and started, devices exist after setup, timer is cancelled on unload.
- `tests/test_coordinator.py` — **new file.** Device creation, rename, add, remove, and the two poll-failure rows from the I/O matrix.

## Tasks & Acceptance

**Execution:**
- [x] `custom_components/securityspy/entity.py` — add `hub_device_info()` and `camera_device_info()`, both returning `homeassistant.helpers.device_registry.DeviceInfo` per the AD-5 identifiers/`via_device`/`entry_type` rules above. Pure, synchronous, fully typed, no imports beyond `homeassistant.helpers.device_registry` and `aiosecurityspy` model types.
- [x] `custom_components/securityspy/const.py` — add `RECONCILE_INTERVAL: Final[timedelta]` (see Design Notes for the chosen value and rationale).
- [x] `custom_components/securityspy/coordinator.py` — add `SecuritySpyDataUpdateCoordinator(DataUpdateCoordinator[ServerInfo])`:
  - `__init__(hass, entry, client, server)`: `super().__init__(hass, LOGGER, config_entry=entry, name=DOMAIN, update_interval=None)`; store `client`; set `self.data = server` via `async_set_updated_data` or direct assignment consistent with `DataUpdateCoordinator` semantics — confirm the correct way to seed initial data without triggering a listener callback with no listeners yet.
  - `async def async_start(self) -> None`: sync the device registry from the **already-seeded `self.data`** — the `ServerInfo` `__init__.py` fetched moments earlier for `test-before-setup` and passed into this constructor. Do **not** issue a second `client.async_get_server_info()` call here: a first-pass implementation did exactly that (an "immediate reconciliation" that re-fetched), and it was a real defect, not a style nit — it wastes a network round-trip on every startup for no reason (`self.data` is already current), and it risks `entry.runtime_data.server` (call #2, fetched in `__init__.py`) silently diverging from `coordinator.data` (a hypothetical call #3) if the account's camera inventory changes in the moment between two back-to-back calls, leaving two different "current server state" objects live in `entry.runtime_data` at once. `async_start` therefore only needs to call the device-registry sync helper directly against `self.data`, then schedule the periodic re-fetch via `async_track_time_interval(self.hass, self._async_reconcile, RECONCILE_INTERVAL)`, registered through `entry.async_on_unload(...)` so cancellation is automatic on unload (do not hand-track the unsub callback). Only the *scheduled* timer ticks call `_async_reconcile()` (and therefore `async_get_server_info()`) again.
  - `async def _async_reconcile(self, *_args) -> None`: call `self.client.async_get_server_info()`; on `SecuritySpyConnectError | SecuritySpyAuthError | SecuritySpyUnsupportedVersionError | SecuritySpyError`, log once at `DEBUG` (per the I/O matrix — this story does not escalate) and return without updating `self.data` or the registry. On success: `async_set_updated_data(new_server)`, then call the same device-registry sync helper `async_start` uses (create/rename/remove) by diffing `new_server.cameras` against the registry's current camera devices for this entry (the registry-diff strategy below is settled, not open).
  - A synchronous or async helper — shared by `async_start` (against `self.data`) and `_async_reconcile` (against `new_server`) — that performs the hub `async_get_or_create` once and the per-camera create/update/remove diff. Extracted so it is unit-testable without going through the timer, and so there is exactly one place this logic exists rather than one for the seed call and a near-duplicate for every re-fetch.
- [x] `custom_components/securityspy/__init__.py` — in `async_setup_entry`, after `entry.runtime_data = SecuritySpyRuntimeData(...)` (extend the dataclass with `coordinator: SecuritySpyDataUpdateCoordinator`), construct the coordinator and `await coordinator.async_start()` before forwarding platform setups. In `async_unload_entry`, no explicit coordinator shutdown call should be needed if the timer unsub is registered via `entry.async_on_unload` — verify and add an explicit `async_shutdown()`/cancel only if `DataUpdateCoordinator` does not already tear down cleanly through unload alone.
- [x] `custom_components/securityspy/__init__.py` docstring — correct the "no coordinator, platform or entity exists yet" claim to reflect what this story adds.
- [x] `tests/conftest.py` — add a fixture building a small `ServerInfo` with 2+ `Camera` entries for coordinator/device tests, and a `device_registry` fixture per the HA test-kit convention if not already provided globally by the test harness.
- [x] `tests/test_coordinator.py` — one test per I/O-matrix row: fresh setup device count (from the seeded data, **no** extra `async_get_server_info()` call — assert the mock client's call count if useful), zero-camera setup, rename, add, remove, host-change non-duplication, transient poll failure, permanent poll failure (this story's failure handling, not 2.8's), timer cancelled on unload, and the periodic timer's callback/interval actually being `_async_reconcile`/`RECONCILE_INTERVAL` (not just that scheduling and cancellation happen).
- [x] `tests/test_init.py` — setup constructs and starts the coordinator; `entry.runtime_data.coordinator` is set and `coordinator.data is entry.runtime_data.server` (same object — no divergent re-fetch); unload cancels the timer (assert via `async_track_time_interval`'s returned unsub being called, or equivalent).
- [x] `tests/test_config_flow.py` — `EXPECTED_VALIDATION_CALLS` stays `2` (`test-before-configure` + `test-before-setup`). It must **not** need to become `3`: if implementing this correctly makes the happy-path flow test see a third call to `async_get_server_info()`, that is a signal the redundant-fetch defect above has been reintroduced, not a reason to raise the constant again.

**Acceptance Criteria** (from epics.md Story 2.3, restated as test targets):
- Given a configured server with N cameras, when setup completes, then exactly one hub device (`entry_type=SERVICE`) and N camera devices exist, each `via_device` the hub, each named from the server with zero manual renaming, and none named after an IP or reporting manufacturer "Generic".
- Given the created devices, when identifiers are inspected, then the hub is keyed by `server.uuid` and each camera by `server.uuid` + `camera.number`, never hostname/IP/name/MAC, and are stable across restarts (re-running setup with the same server produces the same device `id`s, not duplicates).
- Given a camera renamed on the server, when the coordinator next reconciles, then the device name updates and its `id`/`identifiers`/unique IDs (none exist yet, but the invariant must hold once they do) are unchanged.
- Given the server's host/IP/port changes, when the integration reconnects, then no duplicate devices are created.
- Given a camera added or removed on the server, when the coordinator next reconciles, then the corresponding device is added or removed without a manual reload.

## Design Notes

**Why direct `device_registry` calls instead of an entity.** `has_entity_name`/`DeviceInfo`-on-an-entity is the idiomatic way devices normally appear in Home Assistant, and every later platform in this project will use exactly that (see `docs/ha-integration-reference.md` §4). But no per-camera *data* worth exposing as an entity exists before story 2.4 (health) — inventing a placeholder entity (a name sensor, a presence binary sensor with no real signal) just to hang a device off it would be exactly the "more entities is not better" anti-pattern the epic context calls out, and it would need its own translation keys, `entity_category`, and disabled-by-default decision that no requirement asks for yet. `device_registry.async_get_or_create(config_entry_id=...)` creating a device with no entities is a supported, documented pattern for exactly this "device now, entity capability later" situation, and every field this story needs (`entry_type`, `identifiers`, `via_device`, `name`, `manufacturer`, `sw_version`) is available on `DeviceInfo`/the registry API independent of any entity.

**Why `ServerInfo` and not a new `SecuritySpyData` library type.** AD-15 (in the architecture spine, written with the full epic roadmap in view) describes an eventual coordinator container carrying Observation Records, Latest Capture, Detection Episodes, control state, and health per camera — none of which exist in the library yet. `ServerInfo` already is "one frozen, fully-typed container... server state plus a `Mapping[int, Camera]` keyed by camera number" for everything this story's coordinator needs. Introducing a wrapping type now, with fields for data that doesn't exist, would be speculative scaffolding built to a shape nobody can verify yet. The cost of composing/renaming later, when a real epic needs a field `ServerInfo` cannot hold, is small; the cost of guessing that shape now and being wrong is a rewrite either way. Flagged in Boundaries as a decision for review rather than settled fact, because the architecture doc is explicit that this container belongs in the library "from the start" — a reviewer with more context on the epic-4/5 timeline may reasonably override this.

**Why a self-scheduled reconciliation timer instead of waiting for Epic 3.** The epic's own acceptance criteria ("a camera added or removed... reconciles without requiring a reload") describe live reconciliation, but the push stream (Epic 3) and the caplist-driven reconciliation cycle (AD-10, Epics 4-5) don't exist yet. Without *some* scheduled re-fetch, "without a reload" cannot be satisfied honestly in this story — the only truthful alternative would be to narrow the AC to "on next reload," which is not what epics.md says. `RECONCILE_INTERVAL` reuses AD-10's own stated fallback-interval assumption (10 minutes) for consistency, since AD-10 documents that exact number as the intended slow-fallback cadence for this class of problem, even though AD-10 itself is scoped to Observation Records rather than device inventory. This mechanism is explicitly a stand-in: Epic 3's `reconnected` callback and AD-10's real reconciliation cycle will likely subsume or replace this timer, and that migration is out of scope here — noted for the epic-3 dispatch to pick up.

**Diff strategy: registry state, not `self.data`.** Settled by the first implementation/review pass, not left open: diff `server.cameras` against the registry's actual current devices for this config entry, not against the coordinator's previous `self.data.cameras`. Diffing against `self.data` is simpler but drifts from the registry if a device is ever deleted out-of-band (e.g. a user manually removing it in the HA UI) — HA would not recreate it until the coordinator's own view happened to change. The registry-diff is self-healing: a manually-deleted-then-still-present camera is recreated on the next reconciliation rather than staying missing forever. Cover this with a test.

**One sync path, not two.** The first pass wrote the device-registry sync logic once but called it from two different data sources at two different moments — a redundant immediate re-fetch in `async_start` (see the amended `coordinator.py` task above) and the real scheduled re-fetch in `_async_reconcile` — creating a real risk that `entry.runtime_data.server` and `coordinator.data` could diverge right after startup. There is exactly one `ServerInfo` this story needs at startup: the one `__init__.py` already fetched for `test-before-setup`. `async_start` syncs devices from that seeded data directly; only the periodic timer re-fetches and re-syncs. This is not a style preference — a second network round-trip during startup for data that is already current, purely to satisfy an "immediate reconciliation" framing, was the defect a review pass caught.

## Verification

**Commands:**
- `uv run pytest tests/ --cov=custom_components.securityspy --cov-report=term-missing --cov-fail-under=100` — expected: all tests pass, 100% coverage including the new `coordinator.py` and `entity.py` modules.
- `uv run ruff check custom_components tests && uv run ruff format --check custom_components tests` — expected: clean.
- `uv run mypy --strict custom_components/securityspy` — expected: clean (note: `docs/ha-integration-reference.md`'s own coordinator snippet is not `--strict`-clean as written; do not copy it verbatim without adding types).
- `uv run python -m script.hassfest` or the project's equivalent hassfest CI step — expected: clean; confirms `coordinator.py`/`entity.py` satisfy `common-modules`.

**Manual checks (if no CLI):**
- After setup against a real or faked multi-camera server, the HA device registry (Settings → Devices & Services → the entry) shows one hub device and one device per camera, each linked via the hub as parent, with no device named after an IP and no manufacturer "Generic".
- Renaming a camera in SecuritySpy and waiting one reconciliation interval (or manually invoking the coordinator's reconcile method in a dev environment) updates the HA device name without changing its device page URL (i.e. its underlying `id`).

## Auto Run Result

**What was implemented.** Exactly the re-derived Tasks & Acceptance list above, from a clean slate (the repository carried no trace of story 2.3 before this run):

- `custom_components/securityspy/entity.py` (new): pure `hub_device_info(server)` and `camera_device_info(server, camera)`, both returning `DeviceInfo` per AD-5. No entity classes, no HA I/O.
- `custom_components/securityspy/const.py`: added `RECONCILE_INTERVAL: Final[timedelta] = timedelta(minutes=10)` with the AD-10-derived rationale comment.
- `custom_components/securityspy/coordinator.py` (new): `SecuritySpyDataUpdateCoordinator(DataUpdateCoordinator[ServerInfo])`, `update_interval=None`. `__init__` assigns `self.data = server` directly (no `async_set_updated_data`, no fetch). `async_start()` calls the shared `_sync_device_registry(self.data)` helper and schedules `async_track_time_interval(self.hass, self._async_reconcile, RECONCILE_INTERVAL)` through `self.config_entry.async_on_unload(...)` — **it does not call `client.async_get_server_info()`**. `_async_reconcile()` re-fetches, catches `SecuritySpyError` (which already covers `SecuritySpyConnectError`/`SecuritySpyAuthError`/`SecuritySpyUnsupportedVersionError`, so the clause was collapsed to the parent per the review-triage low finding rather than left as four names doing one thing), logs once at `DEBUG` on failure and returns without touching `self.data` or the registry, and on success calls `async_set_updated_data(new_server)` then the same `_sync_device_registry`. The sync helper diffs against the registry's own current devices for this config entry (via `dr.async_entries_for_config_entry`), not against `self.data`, so a manually-deleted device self-heals on the next reconciliation. A `_camera_number_for` helper defensively parses each device's identifiers and skips/removes anything that doesn't decode, rather than raising.
- `custom_components/securityspy/__init__.py`: `SecuritySpyRuntimeData` gained a `coordinator` field. `async_setup_entry` constructs the coordinator and awaits `coordinator.async_start()` after `entry.runtime_data` is set and before `async_forward_entry_setups`. The stale "no coordinator, platform or entity exists yet" docstring line was corrected to describe the post-2.3 state. `async_unload_entry` was left unchanged apart from a comment: no explicit coordinator shutdown call was added, because `ConfigEntry._async_process_on_unload` (verified by reading the installed `homeassistant.config_entries` source) runs every `async_on_unload` callback unconditionally, independent of whether any platform was forwarded.
- Tests: `tests/conftest.py` gained `make_camera` and `make_server_info_with_cameras`. `tests/test_coordinator.py` (new, 15 tests) covers every I/O-matrix row plus the no-redundant-fetch assertion, the timer-cancelled-on-unload assertion, the scheduled-callback/interval assertion, and (for 100% branch coverage) the two unreachable-under-the-invariant malformed-identifier paths in `_camera_number_for`. `tests/test_init.py` gained two tests: coordinator constructed/started with `coordinator.data is entry.runtime_data.server` and exactly one `async_get_server_info` call across the whole setup, and the timer verifiably not firing after unload (via `async_fire_time_changed`, not a mock assertion). `tests/test_config_flow.py` was **not modified**; `EXPECTED_VALIDATION_CALLS` remains `2`.

**No-redundant-fetch confirmation.** `coordinator.async_start()` contains no call to `self.client.async_get_server_info()` — it only calls the synchronous `_sync_device_registry(self.data)` and schedules the timer. This is directly asserted by `tests/test_coordinator.py::test_fresh_setup_registers_hub_and_camera_devices` (`client.async_get_server_info.assert_not_awaited()` after `async_start()`) and by `tests/test_init.py::test_setup_constructs_and_starts_the_coordinator` (`mock_client.async_get_server_info.await_count == 1` across the entire `async_setup_entry` call, and `entry.runtime_data.coordinator.data is entry.runtime_data.server` — the same object, not an equal-but-distinct one). `tests/test_config_flow.py::test_user_flow_creates_entry` continues to assert `EXPECTED_VALIDATION_CALLS == 2` and passes unmodified, which is the regression signal the spec named: had the redundant fetch been reintroduced, that count would need to rise to 3.

**Verification results.**
- `uv run pytest tests/ --cov=custom_components.securityspy --cov-report=term-missing --cov-fail-under=100`: **69 passed**, coverage **100.00%** across all five modules (`__init__.py`, `config_flow.py`, `const.py`, `coordinator.py`, `entity.py`).
- `uv run ruff check custom_components tests` and `uv run ruff format --check custom_components tests`: **clean**.
- `uv run mypy --strict custom_components/securityspy`: **clean** ("Success: no issues found in 5 source files"; one unrelated informational note about an unused `mypy` override section for `pytest_homeassistant_custom_component.*`, pre-existing and not touched by this story).
- `uv run python -m script.hassfest`: **not runnable** in this repository — it is a HACS custom-integration repo, not a Home Assistant core checkout, so `script.hassfest` does not exist locally. The project's actual hassfest gate is the `hassfest` job in `.github/workflows/ci.yml`, which runs `home-assistant/actions/hassfest` against this checkout in CI; it was not executed here. `coordinator.py` and `entity.py` exist under the exact names `common-modules` checks for, which is the only part of that rule this run could self-verify.

**Residual risks / follow-ups.**
- hassfest itself was not run locally (see above) — first real confirmation of `common-modules` and any other hassfest rule is on the next CI run.
- The four low-severity findings from review pass 1 that were "not addressed" (moot, pending re-derivation) were re-evaluated against this implementation: the except-clause collapse and the malformed-identifier defensive handling were both applied directly in this pass; the periodic-timer callback/interval assertion was added as a new test; the auth/unsupported-version poll failures are still retried silently forever at `DEBUG`, which remains explicitly out of scope per this story's own I/O matrix (deferred to story 2.8's auth-failure counter/reauth flow, as before).
- No platform, entity class, or permission gating was added, per the Boundaries — `PLATFORMS` remains `[]`, to be filled in by story 2.4.

**Review pass 2 (patch findings applied directly, no further loopback).** Neither reviewer found an intent_gap or bad_spec this pass. Three low-severity patches were auto-fixed: `_async_reconcile` now wraps `_sync_device_registry` in its own `try`/`except Exception` (logs once at `ERROR`, returns without publishing, so an unmodelled bug can't silently kill future reconciliation cycles); `async_set_updated_data` is now called *after* the registry sync succeeds rather than before, so a listener can never observe `coordinator.data` ahead of the actual device registry; and the misleadingly-named `test_host_change_reload_does_not_duplicate_devices` was renamed with a docstring explaining that no host/IP field exists at this layer to actually change (AD-5). One low finding — `async_start()`'s registry sync running outside any exception-mapping — was logged to `deferred-work.md` rather than fixed, since it is a real gap in the module's stated "one exception-mapping seam" but low practical risk (device-registry writes are local/synchronous, unlike the network failures that seam actually guards against). Eight findings were rejected as speculative, already-settled Design Note tradeoffs, or already handled by existing code — see the Review Triage Log for the full breakdown.

**Verification after pass 2's patches (all green):**
- `uv run pytest tests/ --cov=custom_components.securityspy --cov-report=term-missing --cov-fail-under=100`: **70 passed**, coverage **100.00%**.
- `uv run ruff check custom_components tests` and `uv run ruff format --check custom_components tests`: **clean**.
- `uv run mypy --strict custom_components/securityspy`: **clean**, 5 source files.

**Files changed in pass 2 (beyond the pass-1 re-derivation already listed above):**
- `custom_components/securityspy/coordinator.py` — added the second `try`/`except Exception` around `_sync_device_registry`; reordered `async_set_updated_data` to follow the sync.
- `tests/test_coordinator.py` — added `test_reconcile_survives_an_unexpected_registry_sync_failure`; renamed and re-documented the host-change test.
- `_bmad-output/implementation-artifacts/deferred-work.md` — one new entry for the exception-mapping gap noted above.

## Spec Change Log

### 2026-09-12 — bad_spec repair (review pass 1)
- **Triggering finding:** The first implementation pass's `async_start()` ran an *immediate reconciliation* that re-fetched `async_get_server_info()` a third time (after `__init__.py`'s pre-existing `test-before-setup` call and the config-flow's own validation call), even though that fetch's result (`server`) was already passed into the coordinator's constructor and seeded onto `self.data`. Both independent reviewers (adversarial and edge-case) converged on the resulting risk from different angles: a wasted startup round-trip, and `entry.runtime_data.server` vs. `coordinator.data` silently diverging if the inventory changed between two back-to-back calls.
- **What was amended:** The `coordinator.py` task bullet for `async_start()` now specifies syncing devices from the already-seeded `self.data` directly, with no re-fetch; only the periodic timer's `_async_reconcile()` re-fetches. The device-registry sync logic is now explicitly one shared helper called from both places. The Design Notes' diff-strategy section gained a "One sync path, not two" note recording why. `tests/test_config_flow.py`'s task bullet now pins `EXPECTED_VALIDATION_CALLS` at `2` and calls out a reintroduced third call as a regression signal, not a reason to raise the constant.
- **Known-bad state avoided:** A correct-looking implementation that passes 100% coverage and all typed/lint gates while still making an unnecessary network call on every integration startup and holding two independently-fetched `ServerInfo` objects live in `entry.runtime_data` at once.
- **KEEP instructions (preserve on re-derivation):** `entity.py`'s pure `hub_device_info()`/`camera_device_info()` builders, unchanged. `coordinator.py`'s overall shape (`DataUpdateCoordinator[ServerInfo]`, `update_interval=None`, `async_start`/`_async_reconcile`/a shared device-registry-sync helper). The registry-diff (not `self.data`-diff) self-healing strategy, now settled rather than open. The `__init__.py` docstring correction and the `async_unload_entry` no-explicit-shutdown reasoning. All test coverage for the I/O-matrix rows, adjusted only where the fixed `async_start` behavior changes what they assert (e.g. `coordinator.data is entry.runtime_data.server` after setup, since nothing replaces it anymore).

## Review Triage Log

### 2026-09-12 — Review pass 1
- intent_gap: 0
- bad_spec: 1: (high 0, medium 1, low 0)
- patch: 3: (high 0, medium 0, low 3)
- defer: 1: (high 0, medium 0, low 1)
- reject: 3: (high 0, medium 0, low 3)
- addressed_findings:
  - `[medium]` `[bad_spec]` `async_start()`'s "immediate reconciliation" re-fetched `async_get_server_info()` a third time even though `__init__.py` had just fetched the same data and passed it into the constructor (seeded as `self.data`) — a wasted startup round-trip and a real divergence risk between `entry.runtime_data.server` and `coordinator.data`. Spec amended (see Spec Change Log) to sync the initial device registration from the already-seeded data with no re-fetch; code reverted for re-derivation via step-03.
- Not addressed this pass (moot per cascading order — bad_spec found, so lower-severity findings are re-evaluated against the re-derived diff in the next pass):
  - `[low]` `[patch]` Camera-number extraction in the device-registry diff (`int(identifier.removeprefix(prefix))` / `next(iter(device.identifiers))`) has no defensive handling for a malformed or multi-identifier device; currently unreachable under the sole-writer invariant, but would crash the whole reconciliation rather than skip one bad entry if that invariant were ever violated.
  - `[low]` `[patch]` The reconcile `except` clause lists `SecuritySpyConnectError`/`SecuritySpyAuthError`/`SecuritySpyUnsupportedVersionError` alongside their common parent `SecuritySpyError`; all three are already subclasses of it and every branch takes the same action, so the clause collapses to `except SecuritySpyError:`.
  - `[low]` `[patch]` No test exercises the periodic timer's callback actually firing on schedule (only its scheduling call and cancellation are asserted) — add an assertion on the `async_track_time_interval` call's callback/interval arguments.
  - `[low]` `[defer]` A poll failure classified as `SecuritySpyAuthError` or `SecuritySpyUnsupportedVersionError` is retried silently at `DEBUG` forever with no user-visible signal, even though these are permanent conditions rather than transient ones. Real, but explicitly out of scope per this story's own I/O matrix, which defers escalation to story 2.8's auth-failure counter/reauth flow. Logged for that story's dispatch.
  - `[low]` `[reject]` `RECONCILE_INTERVAL` borrows AD-10's 10-minute assumption without an independently-argued cadence for device-inventory reconciliation specifically — already an explicit, flagged Design Note tradeoff; no new information raised.
  - `[low]` `[reject]` Self-healing recreation of a manually-deleted device on the next reconciliation — already an explicit, flagged Design Note tradeoff.
  - `[low]` `[reject]` Theoretical reentrancy of overlapping reconciliations — no concrete failure scenario at a 10-minute cadence against a single HTTP call.

### 2026-09-12 — Review pass 2
- intent_gap: 0
- bad_spec: 0
- patch: 3: (high 0, medium 0, low 3)
- defer: 1: (high 0, medium 0, low 1)
- reject: 8: (high 0, medium 0, low 8)
- addressed_findings:
  - `[low]` `[patch]` `_async_reconcile` had no defensive handling for an unexpected (non-`SecuritySpyError`) failure from `_sync_device_registry` — such a bug would have escaped the periodic timer callback uncaught. Added a second `try`/`except Exception` around the sync call, logging once at `ERROR` via `LOGGER.exception` and returning without publishing, so the next scheduled attempt still retries. Covered by a new test (`test_reconcile_survives_an_unexpected_registry_sync_failure`).
  - `[low]` `[patch]` `_async_reconcile` called `async_set_updated_data(server)` before `_sync_device_registry(server)`, so a listener could in principle observe `coordinator.data` ahead of the device registry it describes. Reordered: the registry is synced first, and `async_set_updated_data` is only called after that succeeds.
  - `[low]` `[patch]` `test_host_change_reload_does_not_duplicate_devices`'s name/docstring implied an actual host/IP change was exercised, but the test only constructs two coordinators against the identical `server` object. Renamed to `test_a_reload_against_the_same_identity_does_not_duplicate_devices` with a docstring explaining that no host/IP field exists at this layer to change (AD-5), so this is the strongest statement obtainable here.
  - `[low]` `[defer]` `coordinator.async_start()`'s initial `_sync_device_registry` call runs outside any exception mapping, unlike the rest of `__init__.py`'s stated single exception-mapping seam — a failure there would propagate as a raw exception out of `async_setup_entry`. Logged to `deferred-work.md`; low practical risk since device-registry writes are local and synchronous, unlike the network failures the existing seam guards against.
  - `[low]` `[reject]` `server.cameras` could hypothetically be `None` — contradicts the library's actual typed contract (`Mapping[int, Camera]`, default empty mapping, never `None`).
  - `[low]` `[reject]` Direct `self.data = server` assignment ordering relative to `super().__init__()` called "fragile to a future base-class refactor" — no concrete failure, speculative about upstream HA internals changing.
  - `[low]` `[reject]` `test_setup_constructs_and_starts_the_coordinator` mocks `async_start` so it alone doesn't prove no-refetch — true but redundant: the no-refetch behavior is already directly and thoroughly covered by dedicated `test_coordinator.py` tests.
  - `[low]` `[reject]` Malformed/zero-identifier device under this entry not defended against — already handled by the existing `_camera_number_for` fallback (returns `None` → treated as stale → removed) and covered by a dedicated test.
  - `[low]` `[reject]` `RECONCILE_INTERVAL` borrowing AD-10's cadence without independent justification — repeat of a pass-1 finding, already an explicit, flagged Design Note tradeoff.
  - `[low]` `[reject]` A UUID containing an underscore could misparse the identifier prefix — UUIDs are hyphenated hex per RFC 4122 and never contain underscores; factually unfounded.
  - `[low]` `[reject]` A negative camera number would be misparsed as non-numeric and deleted every cycle — speculative, no evidence SecuritySpy ever reports negative camera numbers.
  - `[low]` `[reject]` No reentrancy guard against overlapping reconciliations — repeat of a pass-1 finding; still no concrete failure scenario at this cadence.

