---
updated: 2026-09-13T09:08
---

- (dynamic) First session. Room split three ways on AD-7 (Quinn: amend + restore-dance; Victor/Sally: honest transient control; Dana: split override vs schedule). Jensen took Dana's split.
- (outcome) Jensen: remove the override default. Shipped as b40b52d2 — override is now required keyword-only; 934 lib + 54 integration tests green.
- (outcome) Jensen ruled schedules and overrides are separate concepts — the AD-7 resolution direction. Not yet written into AD-7 or FR-12, so Epic 6 stays blocked.
- (moment) Murat killed the restore-dance in one line: nobody restores the schedule when HA is uninstalled mid-disarm. Quinn had no answer.
- (callback) Paige: the method is still named async_set_camera_arming while doing target+override. 'We'll do this again in November.' Grumbal says it'd be the third time.
- (outcome) AD-7 split codified 73153477: arm switches = transient override; schedule assignment = separate explicit action; schedule DEFINITIONS still untouchable. PRD FR-12 split into FR-12/FR-12a, FR-13 rewritten. Story 1.16 closed.
- (dynamic) Jensen cut the room off for re-litigating a call she'd already made plainly. Lesson for next session: when she states a decision, codify it — don't re-open it.
- (outcome) Jensen's framing that settled the scope line: a schedule is a SecuritySpy-owned object HA interacts with, like a camera — HA doesn't create cameras there, so it doesn't create schedules. Assignment in, definition management out (revisit on appetite). Now in AD-7 + Deferred.
- (outcome) AD-20 born from Jensen's line 'we aren't trying to duplicate SecuritySpy's UI': SecuritySpy owns creation, HA interacts. Promoted out of AD-7 to project level — spine AD-20, AGENTS.md non-negotiable #1, PRD 10.0. Test for any new surface: creating a SecuritySpy object, or acting on one that exists?
- (outcome) Session closed on AD-20 — Jensen wanted the door shut on a principle boundary being written, not on a keepsake. Four commits: b40b52d2, 73153477, a5b64d8d, 899e0ad5.
- (moment) Walk-ons who earned their turns and aren't in the default roster: Grumbal (adversary), Dana (pragmatist), Yui (craftsman), Vex. Grumbal's 'this will page someone at 3am' set the room's temperature; Dana's split became the decision.
- (dynamic) Session 2 opens on story 2.6 live video. Jensen's lean: scoped RTSP token must come from the user (URL Generator is GUI-only). Orchestrator's 2.6 halt had cited stale 'token unverified' text; 6.21 verification §5.16.1 already verified tokens and listed 3 options (paste token / HA proxies / creds-in-URL rejected).
- (moment) Jensen reframed 2.6: goal is video as an entity on the camera device, 'what would we inherit from ONVIF'. Reading onvif/camera.py showed ONVIF's stream_source() embeds user:pass in the RTSP URL (url.with_user/with_password) - the exact pattern AD-13 fights. Room split on whether library-built userinfo URL satisfies AD-13's 'constructed only inside the library' rule.
- (outcome) Live HA check: Jensen's real video setup is NOT ONVIF (11 onvif entries disabled) but 11 Generic Camera entries: rtsp://<server>:8000/stream?cameraNum=N(0-based)&vcodec=h26x&acodec=src&auth=!token, no user/pass, no still URL, tcp, framerate 2; entity ids named after the IP. Token 8-hex prefix shared across cameras (confirms research). Room's read: 2.6 reduces to 'where does the per-camera token come from and where is it stored' plus port-rtsp parsing and acodec=src verification.
- (outcome) DECISION (Jensen): 2.6 uses SecuritySpy's base64 auth= form for the RTSP URL - local network, it's what SecuritySpy provides; zero user setup. Mandate: 'our ever loving best' to keep it out of logs. Supersedes the token-paste direction. AD-13 widening item 3 (never construct base64 form) must be amended - codify, don't re-litigate. Leak audit: HA stream redact_credentials already strips auth=; aiosecurityspy redact_url strips auth=; go2rtc stdout relayed verbatim to HA log (unredacted) is the one real hole - unproven either way, her live log clean in last 2000 lines.
- (outcome) Jensen picked Option A: aiosecurityspy-owned local RTSP relay; go2rtc gets rtsp://127.0.0.1:<port>/<random id>, relay attaches base64 auth= upstream. Now designing execution + what a non-HA library consumer must bring. Split proposed: library relay story first, 2.6 blocked on it. Room angle: relay bind address configurable so external go2rtc works without falling back to the raw URL.
- (outcome) Jensen decided: (1) relay IDs per camera (stable across go2rtc reconnects, rotate on restart). (2) Library exposes unsecured_stream_url - credential-free RTSP URL; caller attaches auth themselves (2-step), so the library never publicly emits a credential-bearing URL. Joked about a consent property; room kept it as a running bit (i_understand=True) but the 2-step wins. Grumbal-Vex 'they'll build it from the README anyway' fight left unresolved.
- (moment) Jensen reopened FR-22 herself: relay is elegant enough she'd use it over her Generic setup, so maybe NOT disabled by default. Her lean, not yet a decision. Note her live setup is Generic cameras, not ONVIF - FR-22's original ONVIF-coexistence premise doesn't match her system.
- (outcome) DECISION (Jensen): camera entities ENABLED by default, no options toggle, README note only (option a). Reason: a fresh SecuritySpy user probably has no video streams in HA yet; migrators like her just disable their old ones. FR-22 rewrite, SM-8 rewrite, PRD line 810 and ha-integration-reference.md:286 need amending. Still gated by 2.7 live-video permission.
- (outcome) Jensen revised to option (b): camera entities enabled by default PLUS an options-flow toggle to skip creating them. Supersedes (a). 2.6 thereby introduces the integration's first options flow (config_flow 100% coverage rule applies).
- (outcome) Jensen approved: toggling create_camera_entities off REMOVES camera entities from the registry, reloads, and stops relay IDs for them; on re-enable same unique IDs return. Session design complete: relay (A), per-camera IDs, unsecured_stream_url 2-step, enabled by default + toggle (b). Pending: write spine AD-13 amendment, PRD FR-22/SM-8, epics 2.6 + new library relay story, revised 2.6 spec. Jensen's open items: URL Generator all-cameras check, optional forum post to Ben.
